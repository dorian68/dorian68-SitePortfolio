#Push to github

git init
git remote -a SitePortfolio
git remote add origin https://github.com/dorian68/SitePortfolio.git
git add .
git commit-m"input your message here"

git push origin master

note (git checkout gives you the list of active branch in the current project)